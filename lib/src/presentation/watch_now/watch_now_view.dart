import 'package:flutter/material.dart';

class WatchNowView extends StatelessWidget {
  const WatchNowView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Watch Now View"),
      ),
    );
  }
}
